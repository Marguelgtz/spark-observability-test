{
  "name": "@spark/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply spark --local",
    "db:migrate:remote": "wrangler d1 migrations apply spark --remote"
  },
  "dependencies": {
    "@spark/core": "workspace:*",
    "@spark/dashboard-contracts": "workspace:*",
    "@spark/github": "workspace:*"
  }
}
